import { createGlobalStyle } from "styled-components";

const SCGlobalStyles = createGlobalStyle`
  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;    
  }
  body {
    background-color: #FB6B6B;
  }
`;

export default SCGlobalStyles;